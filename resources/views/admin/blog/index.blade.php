@extends('admin.layouts.app')
@section('title','Blog')
@section('breadcrumb','Blog')
@section('content')
<div class="d-flex justify-content-between align-items-center mb-4"><div><h1 class="h3 mb-1">Blog</h1><p class="text-muted mb-0">Publish technical articles and career notes.</p></div><a href="{{ route('admin.blog.create') }}" class="btn btn-primary"><i class="bi bi-plus-lg me-2"></i>New Post</a></div>
<div class="card shadow-sm"><div class="table-responsive"><table class="table table-hover align-middle mb-0"><thead><tr><th>Title</th><th>Category</th><th>Author</th><th>Published</th><th>Status</th><th class="text-end">Actions</th></tr></thead><tbody>
@forelse($blogs as $blog)<tr><td><div class="fw-semibold">{{ $blog->title }}</div><small class="text-muted">{{ Str::limit($blog->excerpt,70) }}</small></td><td>{{ $blog->blogCategory?->name ?? '—' }}</td><td>{{ $blog->author ?: '—' }}</td><td>{{ $blog->published_at?->format('M j, Y') ?? '—' }}</td><td><span class="badge {{ $blog->is_published ? 'text-bg-success' : 'text-bg-secondary' }}">{{ $blog->is_published ? 'Published' : 'Draft' }}</span></td><td class="text-end"><a href="{{ route('admin.blog.edit',$blog) }}" class="btn btn-sm btn-outline-primary"><i class="bi bi-pencil"></i></a><form method="POST" action="{{ route('admin.blog.destroy',$blog) }}" class="d-inline" onsubmit="return confirm('Delete this post?')">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger"><i class="bi bi-trash"></i></button></form></td></tr>@empty<tr><td colspan="6" class="text-center py-5 text-muted">No blog posts found.</td></tr>@endforelse</tbody></table></div><div class="card-footer">{{ $blogs->links() }}</div></div>
@endsection
